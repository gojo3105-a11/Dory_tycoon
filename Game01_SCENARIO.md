# Game01_SCENARIO.md - Factory Runner

`GAME_10_MASTER_PLAN.md`의 Game01 개념을 상세화한 시나리오 문서. 코드/Scene 작업은 하지 않는다 -
구현 방법은 `Game01_IMPLEMENTATION_PLAN.md`에서 다룬다.

## 1. 세계관 / 테마

버려진(또는 야간 가동 중인) 공장의 컨베이어 벨트 위를 MainCharacter가 질주한다. 벨트는 바닥과
천장을 오가며, 곳곳에 기계 팔/박스/증기 배출구 같은 장애물이 놓여 있다. 스토리텔링은 최소화하고
(오프닝 컷씬 없음), "왜 달리는가"는 타이틀 화면의 한 줄 설명(예: "컨베이어가 멈추지 않는다 - 최대한
버텨라")으로 대체한다. 10개 게임 전체가 "공장(Factory)"이라는 세계관을 공유하는 첫 번째 사례로,
이후 게임들도 같은 세계관 안의 다른 구역/부서로 해석될 수 있다(예: Game05는 방어 라인, Game02는
경영 사무소) - 단, 이는 아트/설정상의 연결일 뿐 게임플레이 공유를 의미하지 않는다.

## 2. 플레이어 여정

1. **앱 실행 → 타이틀 화면**: 게임 로고, "시작" 버튼, 최고 기록 표시, 상점 버튼.
2. **게임 시작**: 3-2-1 카운트다운 없이 즉시 달리기 시작(세션이 짧으므로 진입 마찰 최소화).
3. **플레이 중**: 화면 상단에 실시간 거리/코인 카운터만 표시(HUD 최소화 - 몰입 방해 요소 배제).
4. **충돌/낙사 → 게임오버 화면**: 이번 판 거리/코인, 최고 기록 갱신 여부, "다시하기"/"상점"/"타이틀로"
   버튼.
5. **상점**: 누적 코인으로 성장 요소(§6) 구매. 구매 즉시 다음 판부터 적용.
6. **재플레이 루프**: 게임오버 → 다시하기가 기본 동선(상점은 선택적 방문).

## 3. 화면 구성 (UI 흐름)

- **Title**: 로고 / Best Distance / Best Coins / [시작] / [상점] / [설정(사운드 on-off)]
- **Gameplay(HUD)**: 좌상단 거리, 우상단 코인, 중력 전환 가능 구간 진입 시 화면 가장자리에 미묘한
  틴트(방향 안내용, 텍스트 지시 없이 시각 신호로만 처리 - 몰입 유지).
- **GameOver**: 이번 판 결과 / 신기록 배지(해당 시) / [다시하기] / [상점] / [타이틀]
- **Shop**: 보유 코인 / 아이템 목록(스크롤) / 각 아이템 [구매]-[장착]-[장착됨] 3단 상태

기존 `UI/GameUIController.cs`가 이미 이런 이벤트 기반 UI 갱신 패턴(GameManager 이벤트 구독)을
구현하고 있으므로, Shop 화면 추가를 제외하면 대부분 재사용 가능하다(상세는 구현 계획에서 확정).

## 4. 상세 메커닉

- **이동**: 자동 전진(고정 속도, 시간에 따라 완만히 가속 - §5 참고).
- **점프**: 탭 = 즉시 점프(멀티 점프 없음, Game01은 단일 점프로 단순하게 유지 - 이중 점프 등은
  다른 게임에서 필요해지면 `Modules/DoubleJump`로 별도 추가).
- **중력 전환**: 지정 구간(`GravitySwitchZone`) 진입 시 자동으로 중력 방향이 뒤집히며 캐릭터가
  바닥→천장(또는 반대)으로 자연스럽게 이동 - 플레이어가 별도로 조작할 필요 없음(기존 구현 방식
  유지, 진입만으로 트리거).
- **장애물 종류**(초기 3종으로 시작, 추후 확장 가능하게 데이터 기반 설계):
  1. 고정 장애물(점프로 회피)
  2. 상하 왕복 장애물(타이밍 회피)
  3. 중력 전환 직후 배치되는 장애물(전환에 익숙해진 플레이어에게 긴장감 부여)
- **코인**: 벨트 위/공중에 배치, 수집 시 즉시 카운트 증가(로컬 판정만, 서버 검증 없음).
- **충돌 판정**: 장애물과의 충돌 = 즉시 게임오버(관대한 히트박스로 프레임 초반 오조작 방지 -
  실제 콜라이더 크기는 구현 단계에서 튜닝).

## 5. 난이도 곡선

- 시작 속도는 낮게, 생존 시간에 비례해 속도가 완만히 증가(상한선 존재 - 조작 불가능한 속도까지
  가지 않도록 캡).
- 장애물 스폰 간격은 속도에 반비례해 줄어들되, 최소 간격(반응 가능 최소 시간)을 하한으로 둔다.
- 중력 전환 구간은 초반엔 드물게, 일정 거리 이후부터 빈도가 늘어난다.
- 정확한 수치(초당 속도 증가량, 최소 스폰 간격 등)는 GameSpec의 `level.difficulty`/`length` 필드로
  조정 가능하게 설계한다(하드코딩 금지 - CLAUDE.md 원칙).

## 6. 성장 시스템 (상점) 상세

| 항목 | 효과 | 비고 |
|---|---|---|
| 캐릭터 의상 스킨 | 외형만 변경(게임플레이 영향 없음) | MainCharacter 공유 프리팹의 머티리얼/부착물 교체로 구현 - 신규 리깅 불필요 |
| 코인 자석(반경) | 일정 반경 내 코인 자동 흡수 | 영구 강화(장착식, 1개만 활성화) |
| 부활 1회 | 게임오버 시 1회 한정 그 자리에서 부활(광고 시청 등 수익화 연동은 이번 MVP 범위 밖) | 판당 소모형 |

MVP는 "코인 자석"과 "스킨 1종"만 구현하고, 부활 아이템은 향후 확장 항목으로 표시한다(과잉 설계
방지 - CLAUDE.md "최소 범위 구현" 원칙).

## 7. 사운드/이펙트 요구사항 (자산 확보 전이므로 요구사항만 정의)

- 배경음: 반복 재생 가능한 짧은 루프 1곡(공장 분위기, 템포 있는 톤)
- 효과음: 점프, 코인 획득, 중력 전환, 충돌/게임오버 - 4종
- 이펙트: 코인 획득 파티클, 중력 전환 화면 틴트, 충돌 시 짧은 화면 흔들림
- **현재 확보된 사운드 에셋 없음** - 무음으로 우선 완성하고, 라이선스 확인된 무료 SFX를 구할 수
  있을 때 추가한다(제작을 멈추지 않는다 - CLAUDE.md/신규 지침 원칙).

## 8. 필요 에셋 목록 (확보 상태 명시)

| 에셋 | 상태 |
|---|---|
| MainCharacter 3D 모델 + Run/Jump 애니메이션 | **미확보** - 사용자 업로드 이미지 기반 제작 예정, 아직 이미지 미업로드. 확보 전까지 캡슐/박스 프리미티브 임시 캐릭터로 핵심 루프부터 완성한다. |
| 컨베이어 벨트/공장 환경 프롭 | **미확보** - Unity 기본 Primitive(Cube 등) + 단색 머티리얼로 임시 대체, 실제 아트 확정 시 교체 |
| 장애물 3종 모델 | **미확보** - 동일하게 프리미티브 임시 대체 |
| SFX/BGM | **미확보** - §7 참고, 무음으로 우선 완성 |

**에셋이 없다는 이유로 개발을 중단하지 않는다.** 위 표의 "임시 대체"가 실제 구현 계획(Phase 3)의
기본 전제다.

## 9. Game01이 "완료"로 인정되는 조건 (신규 지침 §11 재확인)

시나리오 + 구현계획 + 실제 동작 + 캐릭터(임시 포함) + 맵(임시 포함) + 핵심 루프 + 컴파일 오류 0 +
APK 파일 실존 + `Reports/Game01_Report.txt` 존재 - 이 중 하나라도 빠지면 Game02로 넘어가지 않는다.

## 10. 다음 단계

`Game01_IMPLEMENTATION_PLAN.md` 작성 - 여기서 다음 구조적 질문을 사용자에게 확인한다:
**MainCharacter가 3D 휴머노이드인데 현재 Game01(과 프로젝트 전체)의 물리/카메라는 2D
(Rigidbody2D/Orthographic)로 되어 있다. 3D 모델을 2D 평면에서 움직이는 캐릭터로 쓸지(물리/카메라는
2D 유지, 모델만 3D 메시), 아니면 Game01부터 3D 물리/카메라로 전환할지는 되돌리기 어려운 구조
변경이라 CLAUDE.md 규칙상 반드시 확인 후 진행한다.**
